// Express - Web Server Framework for Node.js
// Babel - Javascript Transpiler (try at https://babeljs.io/repl/)
// Webpack - Module builder, import/export related stuff so you can reference .svg in import or module bundling
// Create-react-app - Boilerplate for React so you can avoid setup
// electron-react-boilerplate - Boilerplate for Electron that supports React
// Redux - Global state manager
// ESLint - Linter that auto formats code according to rules
// React-Semantic-UI - SemanticUI based UI Library built for React
// Jest - Unit-testing
// Enzyme - Testing JSX/UI components in a jest unit-test
// SuperAgent - Library to make REST calls
// SuperTest - Test Library to handle REST calls
// Nock - Mock server for unit-testing
// Puser-js: Javascript client to listen/subscribe to pusher notifications/messages
// Pusher: (github project is pusher-http-node) This is for NodeJS to publish messages to pusher
// Flow: Static type checker for javascript
// react-router: Client side router for React applications (can be used server-side too for sever rendered JSX)
// cors: Stands for Cross Origin Resource Sharing. We added this as a middleware to express app

//   boundMethod(contextThis) {
//       return function(event) {
//           this.handleMessageInput(contextThis, event)
//       }
//   }

//   add(val=0, offset=10) {
//       return val + offset
//   }

//   // Currying
//   addWithOffset(offset=10) {
//       return function(val) {
//           return val + offset
//       }
//   }

// test() {
//     let addMethod = this.addWithOffset(100);

//     let response = addMethod(1);
//   }
